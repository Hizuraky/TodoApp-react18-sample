import * as React from 'react';
import { Layout } from '../components/Layout';

export const Login = () => {
  return (
    <Layout title="Log in to your account">
      <div />
    </Layout>
  );
};
